* elvis operator not working?
* accessing empty array by index 0 what should be the behavior?
* support async, await, setTimeOut() and ES comppliant promises
* support ES module loading, import and export keywords